opencv-python==4.13.0.90
face_recognition==1.3.0
numpy>=2.0.0
pandas>=3.0.0

# IMPORTANT: To install the dependencies on Windows, you need CMake installed system-wide.
# Download from: https://cmake.org/download/
# OR install Visual Studio C++ build tools which includes CMake
# After CMake is installed, run: pip install -r requirements.txt
